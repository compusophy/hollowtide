# Copy this file to .env and fill in secrets.
# .env is gitignored; .env.example is not.

# Railway project token for the `railway` CLI (project-scoped, not account-wide).
# Get it from: Railway dashboard → project → Settings → Tokens
RAILWAY_TOKEN=
